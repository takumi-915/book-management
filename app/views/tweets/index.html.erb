  <% @tweets.each do |tweet| %>

    <div class="sidebar">
      ログインしているuser一覧
     <%= tweet.name %>
    </div>

    <div class="task">
      <span class="name">
        <%# <% if user_signed_in? %> %>
          <%# <%= current_user.nickname %>  %>
        <%# <% end %> %>
      </span>
      <div class="content">
      ここにjavascript埋め込む
      </div>
      <div class="comment">
        <%= simple_format(tweet.text) %>
        <%= tweet.created_at %>
      </div>
    </div>

  <% end %>