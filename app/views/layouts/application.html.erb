<!DOCTYPE html>
<html>
  <head>
    <title>Book Manegiment</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
  <body>
    <header class="header">
      <div class="header__title">
        <%= link_to "Book Manegement", root_path, class: "header__title--text" %>
      </div>
      <div class="header__right">
        <% if user_signed_in? %>
          <%= link_to "New Post", new_post_path, class: "header__right--btn" %>
          <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "header__right--btn" %>
        <% else %>
          <%= link_to "Login", new_user_session_path, class: "header__right--btn" %>
          <%= link_to "Signup", new_user_registration_path, class: "header__right--btn" %>
        <% end %>
      </div>
    </header>
    <div class="contents">
    　<%= yield %>
    </div>
  </body>
</html>
